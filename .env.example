# =============================================================================
# ENVIRONMENT SECRETS
# =============================================================================
# File ini berisi CREDENTIALS/SECRETS untuk akses external services.
# Copy ke .env dan isi dengan nilai yang sesuai.
#
# ⚠️  File .env tidak di-commit ke git (sudah di .gitignore)
# =============================================================================

# ---------------------------------------------------------------------------
# GOOGLE OAUTH (OPSIONAL - untuk login dengan Google)
# ---------------------------------------------------------------------------
# Setup: https://console.cloud.google.com/apis/credentials
# 
# 1. Create OAuth 2.0 Client ID (Web application)
# 2. Tambahkan redirect URIs:
#    - http://localhost:5173/auth/google/callback (dev)
#    - https://yourdomain.pages.dev/auth/google/callback (prod)
# ---------------------------------------------------------------------------
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ---------------------------------------------------------------------------
# RESEND EMAIL (OPSIONAL - untuk email verification & reset password)
# ---------------------------------------------------------------------------
# Get API key: https://resend.com/api-keys
# 
# NOTE: Domain di Resend tidak punya inbox. Gunakan REPLY_TO_EMAIL 
# dengan Gmail/Outlook untuk menerima balasan user.
# ---------------------------------------------------------------------------
RESEND_API_TOKEN=re_your_api_token
FROM_EMAIL=noreply@yourdomain.com
REPLY_TO_EMAIL=support@yourdomain.com

# ---------------------------------------------------------------------------
# S3-COMPATIBLE STORAGE (OPSIONAL - untuk file upload)
# ---------------------------------------------------------------------------
# Support: Cloudflare R2, Wasabi, AWS S3, MinIO, DigitalOcean Spaces, dll
#
# Kenapa perlu ini? Untuk generate presigned URLs (upload langsung dari 
# browser tanpa melalui server) dan operasi file via S3 API.
#
# Supported providers:
#   - Cloudflare R2: https://<account_id>.r2.cloudflarestorage.com
#   - Wasabi:        https://s3.wasabisys.com (atau region-specific)
#   - AWS S3:        https://s3.<region>.amazonaws.com
#   - MinIO:         http://localhost:9000 (atau server MinIO Anda)
#   - DO Spaces:     https://<region>.digitaloceanspaces.com
#
# Get credentials:
#   - R2:     https://dash.cloudflare.com > R2 > Manage R2 API Tokens
#   - Wasabi: https://wasabi.com > Access Keys
#   - AWS:    https://console.aws.amazon.com > IAM > Access Keys
#   - MinIO:  Console > Access Keys
# ---------------------------------------------------------------------------

# --- Cloudflare R2 (default) ---
# S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
# S3_BUCKET_NAME=my-bucket
# S3_ACCESS_KEY_ID=your_r2_access_key_id
# S3_SECRET_ACCESS_KEY=your_r2_secret_access_key
# S3_PUBLIC_URL=https://pub-<hash>.r2.dev
# S3_REGION=auto

# --- Wasabi ---
# S3_ENDPOINT=https://s3.us-east-1.wasabisys.com
# S3_BUCKET_NAME=my-bucket
# S3_ACCESS_KEY_ID=your_wasabi_access_key
# S3_SECRET_ACCESS_KEY=your_wasabi_secret_key
# S3_PUBLIC_URL=https://s3.us-east-1.wasabisys.com/my-bucket
# S3_REGION=us-east-1

# --- AWS S3 ---
# S3_ENDPOINT=https://s3.ap-southeast-1.amazonaws.com
# S3_BUCKET_NAME=my-bucket
# S3_ACCESS_KEY_ID=your_aws_access_key
# S3_SECRET_ACCESS_KEY=your_aws_secret_key
# S3_PUBLIC_URL=https://my-bucket.s3.ap-southeast-1.amazonaws.com
# S3_REGION=ap-southeast-1

# --- MinIO ---
# S3_ENDPOINT=http://localhost:9000
# S3_BUCKET_NAME=my-bucket
# S3_ACCESS_KEY_ID=your_minio_access_key
# S3_SECRET_ACCESS_KEY=your_minio_secret_key
# S3_PUBLIC_URL=http://localhost:9000/my-bucket
# S3_REGION=us-east-1

# Untuk backward compatibility dengan config lama (R2_*), 
# variabel R2_* masih didukung:
# R2_ACCOUNT_ID=xxx
# R2_ACCESS_KEY_ID=xxx
# R2_SECRET_ACCESS_KEY=xxx
# R2_PUBLIC_URL=xxx
# R2_BUCKET_NAME=xxx
